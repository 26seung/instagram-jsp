
server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: utf-8
      enabled: true

spring:
  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp
  output:
    ansi:
      enabled: ALWAYS

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/photogram?serverTimezone=Asia/Seoul
    username: ENC(OkgkUUf+wfPHM1SqplWkeQ==)
    password: ENC(weNVeQ5bSyaMWPDLx6Y2Sw==)

  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true

  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB

#  security:
#    user:
#      name: test
#      password: 1234

  profiles:
    include: oauth

file:
  path: /Users/euseung/Documents/dev/cloneCoding/photogram/upload/

jasypt:
  secrete-key: ${JASYPT_SECRETE_KEY:}